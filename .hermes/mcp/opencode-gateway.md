# OpenCode to Hermes Gateway

Hermes can expose conversations as an MCP server:

```bash
hermes mcp serve
```

Treat the gateway as optional and review its trust boundary before use. It should never be enabled automatically by the bootstrap.

