---
description: "Refresh repository, runtime, tools, and Git reality evidence"
---

Run the local evidence refresh without implementation changes. Preserve `$ARGUMENTS` as data. Use the installed extension launcher with `--phase reality --runtime opencode --json`, inspect only the current project, and report the JSON decision plus any tool gaps. Do not infer GREEN from a missing or invalid bridge response.
