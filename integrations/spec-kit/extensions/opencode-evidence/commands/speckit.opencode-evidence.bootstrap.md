---
description: "Run the existing project bootstrap in dry-run mode"
---

Run the existing project bootstrap in dry-run mode only. Do not pass `--apply`, `--include-remote-ci`, commit, push, or alter global configuration. Use `$ARGUMENTS` only as a reviewed target-data value and stop on invalid or missing output.
